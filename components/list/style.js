// External modules
import { css } from '@emotion/react';

// Internal modules
import { flexCenterStyle } from '@/styles/shared';

export const listStyle = css`
  ${flexCenterStyle}
  flex-wrap: wrap;
  gap: 35px;
  padding: 0;
  margin: 0;
  list-style: none;
`;
